# 05-性能测试

本页只记录当前仓库真实存在的 benchmark 源文件、target、命令、参数与复现要求。

## 当前证据状态

- 本次文档整改**没有**附带新的 benchmark 原始输出快照。
- 因此，本页**不再给出不可复核的延迟 / QPS 数字**。
- 当前状态应理解为：**真实 benchmark 清单 + 可复现命令 + “尚未在本次整改中重新测量” 的说明**。

若要把结果升级为可审计结论，请至少同时保存：

- commit / 分支信息
- 机器规格、OS、编译器版本
- CMake 配置选项
- benchmark 命令行
- benchmark 原始 stdout / stderr

补充说明：`B1` / `B2` / `B3` 当前打印的 `Build Timestamp` 来自编译期宏 `__DATE__` / `__TIME__`，表示**二进制构建时间**，不是本次测量实际发生时间；真实测试时间请在运行命令时另行记录。

## 1. Benchmark target 清单

| 来源文件 | CMake target | 默认参数 | 运行前提 | 必需环境变量 |
| --- | --- | --- | --- | --- |
| `benchmark/B1-stdio_performance.cc` | `B1-stdio_performance` | `iterations=1000` | 需要一个双向 stdio MCP 服务端 | 无 |
| `benchmark/B2-http_performance.cc` | `B2-http_performance` | `--url http://127.0.0.1:8080/mcp --connections 32 --requests 1000 --io 4 --compute 2` | 需要一个正在运行的 HTTP MCP 服务端 | 无 |
| `benchmark/B3-concurrent_requests.cc` | `B3-concurrent_requests` | `--url http://127.0.0.1:8080/mcp --workers 10 --requests 100` | 需要一个正在运行的 HTTP MCP 服务端 | 无 |

## 2. 构建命令

```bash
cmake -S . -B build -DBUILD_MODULE_EXAMPLES=OFF
cmake --build build --target B1-stdio_performance B2-http_performance B3-concurrent_requests T2-stdio_server T4-http_server
```

## 3. `B1-stdio_performance`

- 来源：`benchmark/B1-stdio_performance.cc`
- target：`B1-stdio_performance`
- 测试项：`ping`、`tools/call`、`resources/read`、`tools/list`、`resources/list`、`prompts/list`
- 当前文档状态：**仅保留命令，不提供本次整改新结果**

### 手工运行

```bash
mkfifo /tmp/galay-b1-c2s /tmp/galay-b1-s2c
./build/bin/T2-stdio_server < /tmp/galay-b1-c2s > /tmp/galay-b1-s2c &
SERVER_PID=$!
./build/bin/B1-stdio_performance 1000 > /tmp/galay-b1-c2s < /tmp/galay-b1-s2c
kill ${SERVER_PID}
rm -f /tmp/galay-b1-c2s /tmp/galay-b1-s2c
```

### 可记录的最小证据链

- `uname -a`
- 编译器版本
- `cmake -S . -B build ...` 的配置输出
- 上述 benchmark 原始输出

## 4. `B2-http_performance`

- 来源：`benchmark/B2-http_performance.cc`
- target：`B2-http_performance`
- 默认 URL：`http://127.0.0.1:8080/mcp`
- 默认并发：`32 connections`
- 默认请求数：`1000 requests / connection`
- 默认 runtime：`io=4`、`compute=2`
- 当前文档状态：**仅保留命令，不提供本次整改新结果**

### 启动服务端

```bash
./build/bin/T4-http_server 8080 0.0.0.0
```

### 运行 benchmark

```bash
./build/bin/B2-http_performance \
  --url http://127.0.0.1:8080/mcp \
  --connections 32 \
  --requests 1000 \
  --io 4 \
  --compute 2
```

## 5. `B3-concurrent_requests`

- 来源：`benchmark/B3-concurrent_requests.cc`
- target：`B3-concurrent_requests`
- 默认 URL：`http://127.0.0.1:8080/mcp`
- 默认参数：`--workers 10 --requests 100`
- 可选模式：`--scalability`
- 当前文档状态：**仅保留命令，不提供本次整改新结果**

### 基本运行

```bash
./build/bin/B3-concurrent_requests \
  --url http://127.0.0.1:8080/mcp \
  --workers 10 \
  --requests 100
```

### 可扩展性模式

```bash
./build/bin/B3-concurrent_requests \
  --url http://127.0.0.1:8080/mcp \
  --scalability
```

## 6. 如何把“历史页”升级为“当前页”

只有满足以下条件时，才建议重新在文档里写具体数字：

1. benchmark 命令刚刚在当前 commit 上完成
2. 原始输出已保存
3. 硬件 / OS / 编译器信息已记录
4. 服务端 target、URL、参数与页面完全一致
5. 页面明确写出“结果日期”和“结果对应 commit”

在满足以上条件前，请继续把本页当作运行说明，而不是性能承诺。

## 7. 相关文档

- [00-快速开始](00-快速开始.md) - 启动 `T2-stdio_server` / `T4-http_server` 的最短路径
- [04-示例代码](04-示例代码.md) - 测试 target、回归脚本和联调命令
- [07-常见问题](07-常见问题.md) - 结果解释与排障边界
